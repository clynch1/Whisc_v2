<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="whisc.whisc_v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginPage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccountSettings"
            android:label="@string/title_activity_account_settings" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_main_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CreateAccount"
            android:label="@string/title_activity_create_account" />
        <!-- <activity android:name=".MainActivity" /> -->
        <!-- <activity android:name=".LoginPage" /> -->
        <!-- <activity android:name=".CreateAccount"></activity> -->
        <activity android:name=".ViewMeal" />
        <activity android:name=".AddMeal" />
        <activity android:name=".EditMeal" />
        <activity android:name=".AddIngredient" />
        <activity android:name=".EditIngredient" />
        <activity android:name=".AccountSettingsHolder" />
        <activity android:name=".ViewMatch"></activity>
    </application>

</manifest>